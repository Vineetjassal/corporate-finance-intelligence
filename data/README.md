# Data

Place normalized company financial snapshots here as JSON files matching the schema demonstrated by `sample_company.json`.

All monetary fields should use the same unit within a file. The toolkit does not infer currency or units.
